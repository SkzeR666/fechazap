import { createHmac,timingSafeEqual } from 'node:crypto';import { env } from '../config';
export function validMpSignature(signature:string|null,requestId:string|null,dataId:string|null){const secret=env().MERCADO_PAGO_WEBHOOK_SECRET;if(!secret||!signature||!requestId||!dataId)return false;const parts=Object.fromEntries(signature.split(',').map(x=>x.split('=')));if(!parts.ts||!parts.v1)return false;const expected=createHmac('sha256',secret).update(`id:${dataId.toLowerCase()};request-id:${requestId};ts:${parts.ts};`).digest('hex');return expected.length===parts.v1.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(parts.v1))}
export async function mpGet(path:string){const token=env().MERCADO_PAGO_ACCESS_TOKEN;if(!token)throw new Error('mercado_pago_not_configured');const r=await fetch(`https://api.mercadopago.com${path}`,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw new Error(`mercado_pago_${r.status}`);return r.json()}
export async function mpPost(path:string,body:unknown,idempotencyKey:string){const token=env().MERCADO_PAGO_ACCESS_TOKEN;if(!token)throw new Error('mercado_pago_not_configured');const r=await fetch(`https://api.mercadopago.com${path}`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json','X-Idempotency-Key':idempotencyKey},body:JSON.stringify(body)});if(!r.ok)throw new Error(`mercado_pago_${r.status}_${await r.text()}`);return r.json()}
